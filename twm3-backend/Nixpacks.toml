[build]
# Build commands
cmds = ["npm ci --omit=dev"]

# Environment variables during build
env = { NODE_ENV = "production" }

# Node version
nixpkgs = ["nodejs_22"]

[start]
# Start command
cmd = "npm start"

[env]
# Production environment
NODE_ENV = "production"
