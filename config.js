// Global configuration
const config = {
    // Replace this with your actual Vercel backend URL
    // Example: https://twm3-backend.vercel.app
    API_BASE_URL: 'https://twm3-backend.vercel.app' 
};

window.appConfig = config;
