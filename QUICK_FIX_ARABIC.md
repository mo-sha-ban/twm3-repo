# الحل السريع للمشكلة 🚀

## المشكلة
الموقع على Hostinger لا يعمل - ملفات JavaScript لا تحمل والـ API يعطي 404

## الحل في 5 خطوات

### 1️⃣ حدث رابط API
افتح ملف `js/config.js` وغير الرابط:

```javascript
API_BASE_URL: 'https://your-project.railway.app/api'
```

غير `your-project` لاسم مشروعك على Railway (أو Vercel)

### 2️⃣ ارفع الملفات على Hostinger

**الملفات المطلوبة:**
- ✅ `.htaccess` (مهم جداً!)
- ✅ `index.html`
- ✅ مجلد `js/` كامل
- ✅ مجلد `css/` كامل
- ✅ مجلد `img/` كامل
- ✅ جميع ملفات HTML

**لا ترفع:**
- ❌ مجلد `node_modules/`
- ❌ مجلد `api/`
- ❌ مجلد `twm3-backend/`

### 3️⃣ تأكد من إعدادات Hostinger

في لوحة التحكم:
1. اذهب لـ Advanced → Apache Configuration
2. تأكد من تفعيل:
   - ✅ mod_rewrite
   - ✅ mod_headers

### 4️⃣ ارفع Backend على Railway/Vercel

**للـ Railway:**
```bash
# ارفع التغييرات على GitHub
git add .
git commit -m "Fix deployment issues"
git push

# أو استخدم Railway CLI
railway up
```

**للـ Vercel:**
```bash
vercel --prod
```

### 5️⃣ اختبر الموقع

افتح المتصفح واضغط F12:
- تحقق من Console - يجب ألا يكون فيه أخطاء حمراء
- تحقق من Network - يجب تحميل ملفات JS بنجاح

## إذا ما زالت المشكلة موجودة

### خطأ MIME type:
```
Refused to execute script because MIME type is 'text/html'
```
**الحل:** تأكد من رفع ملف `.htaccess` في المجلد الرئيسي

### خطأ 404 للـ API:
```
GET https://www.twm3.org/api/courses 404
```
**الحل:** 
1. تحقق من رابط API في `js/config.js`
2. تأكد من أن Vercel deployment يعمل

### الصفحات لا تفتح:
**الحل:** تأكد من:
1. ملف `.htaccess` موجود
2. mod_rewrite مفعل في Hostinger

## اختبار سريع

افتح هذه الروابط في المتصفح:

1. **Frontend:** https://www.twm3.org
2. **API Health:** https://your-project.railway.app/health (أو vercel.app)
3. **API Courses:** https://your-project.railway.app/api/courses

يجب أن تعمل جميعها بدون أخطاء!

## ملاحظات مهمة ⚠️

1. **الفرونت إند على Hostinger فقط** - لا ترفع ملفات Backend
2. **الباك إند على Vercel فقط** - لا ترفع ملفات Frontend
3. **ملف `.htaccess` ضروري** - بدونه لن يعمل الموقع
4. **تحديث رابط API** - يجب أن يشير لـ Vercel

## هل تحتاج مساعدة؟

إذا ما زالت المشكلة:
1. افتح Developer Tools (F12)
2. التقط صورة للأخطاء في Console
3. التقط صورة للأخطاء في Network tab
4. شاركها معي

---

**آخر تحديث:** تم إنشاء جميع الملفات المطلوبة ✅